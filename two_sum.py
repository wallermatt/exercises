from re import I
from typing import List

def get_combinations(nums: List[int], size: int) -> List[List[int]]:
    results = []
    for e in nums:
        for s in range(size):
            


def twoSum(nums: List[int], target: int) -> List[int]:
    for i in range(1, len(nums) + 1):
        print(i)
    return [0]

twoSum([1,2,3], 3)
